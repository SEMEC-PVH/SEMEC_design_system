export const metadata = { title: "Requisitos mínimos" };

export default function RequisitosPage() {
  return (
    <>
      <h1>Requisitos mínimos</h1>
      <p>
        Para utilizar o SEMEC Design System, são necessários React 19, Tailwind
        CSS v4 e os pacotes do shadcn (Radix + CVA + clsx + tailwind-merge).
      </p>
    </>
  );
}
